# Perception overlay: TrackDrive
roi:
  mode: track
road_type: 3

# Keep explicit to avoid semantic scorer mismatches in downstream modules.
filters:
  obstacle_height:
    enable: true
tracker:
  enable: true
topology:
  enable: true
