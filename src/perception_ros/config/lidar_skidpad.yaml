# Perception overlay: Skidpad
roi:
  mode: skidpad
road_type: 1

# Skidpad-specific behavior not covered by mode_presets in code.
filters:
  obstacle_height:
    enable: false
tracker:
  enable: false
topology:
  enable: false
