# Perception overlay: Acceleration
roi:
  mode: accel
road_type: 2

# Keep explicit to avoid semantic scorer mismatches in downstream modules.
filters:
  obstacle_height:
    enable: true
tracker:
  enable: true
topology:
  enable: true
