# ==========================================================
# 感知覆盖模板 (示例文件)
# ==========================================================
# 用途：
#   此文件是覆盖配置的复制粘贴模板。
#   请勿将其作为单一的完整配置使用。
#
# 推荐分层加载顺序：
#   1) lidar_base.yaml               (全局共享默认值)
#   2) lidar_<mode>.yaml             (track/accel/skidpad 模式配置)
#   3) mission overlay               (赛事覆盖，可选)
#   4) vehicle overlay               (车辆覆盖，可选)
#   5) local overlay (*.local.yaml)  (本地覆盖，可选，已gitignore)
#
# 每个覆盖文件应保持精简：
#   - 仅包含与下层不同的键值
#   - 不要重复默认值
# ==========================================================

# ----------------------------------------------------------
# 示例 A：赛事覆盖 (例如 EBS 紧急制动测试)
# ----------------------------------------------------------
# 保存路径：fsd_launch/config/missions/<mission>/perception.yaml
# roi:
#   mode: custom                     # 自定义ROI模式
#   custom:
#     x_min: 0.0                     # X轴最小值 (m)
#     x_max: 25.0                    # X轴最大值 (m)
#     y_min: -4.0                    # Y轴最小值 (m)
#     y_max: 4.0                     # Y轴最大值 (m)
#
# road_type: 3                       # 旧版兼容
#
# tracker:
#   enable: false                    # 关闭时序跟踪
#
# topology:
#   enable: false                    # 关闭拓扑修复


# ----------------------------------------------------------
# 示例 B：车辆覆盖 (安装标定参数)
# ----------------------------------------------------------
# 保存路径：fsd_launch/config/vehicles/<vehicle>/perception.yaml
# imu:
#   sensor:
#     extrinsics:
#       enable: true                 # 启用外参
#       tx: 0.00                     # X方向平移 (m)
#       ty: 0.00                     # Y方向平移 (m)
#       tz: 0.00                     # Z方向平移 (m)
#       roll: 0.0                    # Roll旋转 (度)
#       pitch: 0.0                   # Pitch旋转 (度)
#       yaw: 0.0                     # Yaw旋转 (度)


# ----------------------------------------------------------
# 示例 C：本地覆盖 (临时调参)
# ----------------------------------------------------------
# 保存路径：fsd_launch/config/vehicles/<vehicle>/perception.local.yaml
# confidence:
#   min_confidence_near: 0.2         # 近处置信度门槛
#   min_confidence_far: 0.5          # 远处置信度门槛
#
# filters:
#   obstacle_height:
#     enable: true                   # 启用柱状障碍物滤波


# 空YAML对象，保持文件有效
{}
