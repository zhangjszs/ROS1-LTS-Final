# 未完成计划总结报告

> **报告修订日期**: 2026-03-20
> **修订依据**: 已按仓库代码、launch、测试与脚本现状重新核对
> **说明**: 旧版“已完成/未完成”二元统计已失效。本文改为区分 `已完成`、`部分落地`、`未实现` 三种状态。
> **权威剩余事项清单**: `docs/remaining_work_audit_2026-03-06.md`

---

## 一、总体结论

- `Audit Fixes Progress` 已完成，不应再视为进行中。
- `Vision Module` 不是“代码未开始”，而是“基础代码已落地，模型/数据/标定/验证未完成”。
- `Color Pipeline Gaps` 计划中的 5 个代码改动已基本落地，但端到端语义收口和回归验证仍未完成。
- `Localization M1-M5` 与 `Innovation #1-#3` 不是纯文档草案；`factor_graph` 主后端已可切换并完成首个 mission 回放验收，后续重点转为多 mission 回归矩阵与门禁收口。
- `Planning M1-M4` 仍以设计文档为主，当前未实现核心已转为 `HUAT_PathLimitsV2` 切主验收（控制侧并行兼容+自动回退框架已落地）、Autocross 新主线，以及 M3/M4 的设计收口。

---

## 二、按计划重分类

| 序号 | 计划名称 | 所属模块 | 修订后状态 | 说明 |
|-----|---------|---------|-----------|------|
| 1 | Color Pipeline Gaps Implementation | Planning Core | 部分落地 | 5 个计划改动已有实现痕迹，剩余工作在端到端验证和高必颜色语义收口 |
| 2 | Vision Module Design & Implementation | Perception | 部分落地 | `vision_core` / `vision_ros` / launch 已存在，模型链路与数据资产未完成 |
| 3 | Localization M1 Interface Freeze | Localization | 部分落地 | 接口文档已成形；FG 主后端切换与 `trackdrive` 回放验收已完成，剩余驱动字段与多 mission 收口 |
| 4 | Localization M2 CTF-Graph Design | Localization | 部分落地 | 因子图、颜色代价、异常状态机已有代码，主后端可切换；默认策略仍以稳态兼容为先 |
| 5 | Localization M3 Loop Closure & Reloc | Localization | 部分落地 | 描述子重定位/粒子重定位已有基础实现，系统级验证未完成 |
| 6 | Localization M4 Validation Plan | Localization | 部分落地 | 验证工具与脚本已落地，主要缺标准场景数据与系统级自动化回归 |
| 7 | Localization M5 Risk Checklist | Localization | 部分落地 | 风险清单与配套脚本已落地，仍缺 KPI 阈值与 CI 门禁收口 |
| 8 | Innovation #1 Color-Topology Factor | Localization | 部分落地 | 颜色混淆和拓扑代价已有代码，仍未完成主链路收口 |
| 9 | Innovation #2 Skidpad Circle Factor | Localization | 部分落地 | `CircleConstraintFactor` 与测试已存在，但未成为主定位后端的默认能力 |
| 10 | Innovation #3 Relocalization | Localization | 部分落地 | 重定位代码存在，仍缺系统级验收 |
| 11 | Planning M1 Interface Spec | Planning | 部分落地 | `HUAT_PathLimitsV2` message/转换工具、主链 V1/V2 双发布、控制侧并行订阅+自动回退框架已落地，切主验收未完成 |
| 12 | Planning M2 Autocross Design | Planning | 未实现 | Boundary Graph / Corridor / Centerline Optimizer 仍停留在设计文档 |
| 13 | Planning M3 Linear Accel Design | Planning | 未实现 | 直线加速赛 V2 输出与分段速度规划未落地 |
| 14 | Planning M4 Skidpad Design | Planning | 未实现 | 双圆拟合增强、圈次状态机、相位速度控制未按设计收口 |
| 15 | Planning M5 Validation & Migration | Planning | 已完成但仍有收尾 | Unified pipeline 已验证冻结，仍建议继续做遗留清理和实车验证 |
| 16 | Audit Fixes Progress | System | 已完成 | 22/22 已完成，文档内部旧过程记录不应再作为当前状态引用 |

---

## 三、重点修订说明

### 3.1 感知模块

#### Vision Module Design & Implementation
- **修订前误差**: 旧文档写为“代码实现尚未开始”。
- **修订后判断**: 基础代码已存在，包括：
  - `src/vision_core/`
  - `src/vision_ros/`
  - `src/fsd_launch/launch/subsystems/vision.launch`
  - `perception_ros` 侧 `vision_inject`
- **真正未完成**:
  1. YOLO/ONNX/TensorRT 模型资产
  2. 训练数据与标注流程
  3. 相机标定参数
  4. 带图像 bag 的端到端回放验收

#### Color Pipeline Gaps Implementation
- **修订前误差**: 旧文档写为“待实施”。
- **修订后判断**: 以下计划项已在代码中出现：
  1. `LineDetectionCore` 稀疏锥桶颜色感知 fallback
  2. `SkidpadDetectionCore` 颜色感知圆分配
  3. 相关测试用例补色
  4. 插值锥桶继承邻居颜色
  5. 高速规划 cone type 范围校验
- **真正未完成**:
  1. visualization 对历史包（legacy 枚举）的最终兼容验收

- **2026-03-15 本轮补充**:
  1. 关键代码链路已完成 `LEFT=RED / RIGHT=BLUE` 语义收口（`planning/high-speed`、`localization FG` 入口、`vision` 枚举边界）
  2. 颜色枚举已统一按 `0..4` 处理，并兼容 legacy `5 -> RED`
  3. 新增针对高必左右边界语义的单元测试
  4. 已完成 replay 专项采样并生成 `perf_reports/results/color_semantics_replay_validation_20260315.{json,md}`：
     - `track` 有 fused 流，但侧向一致率约 `49.25%`
     - `accel/skidpad` 在本次回放窗口未录到 fused 消息

- **2026-03-17 本轮补充**:
  1. 已修复 `fusion` 无同步对时的发布缺口，`track/accel/skidpad` 三模式回放均可录到 fused
  2. `accel/skidpad` mission 默认视觉输入统一为 `/resize_img_out`，避免与常用 bag 话题错配
  3. 已完成二次 replay 并生成 `perf_reports/results/color_semantics_replay_validation_20260317.{json,md}`：
     - `track` overall consistency: `0.7582`（由 `0.4925` 提升）
     - `accel` overall consistency: `1.0000`
     - `skidpad` overall consistency: `0.9834`
  4. 已冻结颜色语义门限并落地门禁脚本：
     - `scripts/check_color_semantics_regression_mode.sh`
     - `perf_reports/baselines/perception/{track,accel,skidpad}.color_semantics.thresholds.env`
     - `scripts/check_perception_regression_mode.sh` 已支持 `--color-bag` 串行门禁

- **2026-03-18 本轮补充**:
  1. 已新增 `.github/workflows/perception_color_semantics_ci.yaml`，将颜色语义门禁接入常规 CI（workflow_dispatch + PR/push 路径触发）
  2. CI 中执行门禁资产存在性检查、shell/python 语法检查与 `test_mainline_adapter_launch_contract.py` 合约单测

### 3.2 定位模块

#### Localization M1-M3 / Innovation #1-#3
- **修订前误差**: 旧文档将这些项统一描述为“草案/未实现”。
- **修订后判断**: 代码已有明显基础设施：
  - `factor_graph` backend
  - `CircleConstraintFactor`
  - descriptor / particle relocalization
  - 颜色代价与拓扑代价
- **真正未完成**:
  1. `vehicle_interface_ros` 驱动字段补全（acc/Roll/NSV/Age）
  2. FG 主后端迁移从单 mission 扩展到全 mission 回放矩阵
  3. 主后端切换后的系统级 KPI（精度/抖动/重定位/延迟）门禁化
- **2026-03-20 本轮补充**:
  1. 已新增并跑通 `scripts/validate_localization_fg_mainline_replay.sh`（`backend:=factor_graph` + `fg_shadow_mode:=false`）
  2. 已完成 `trackdrive` 主后端迁移回放验收并 PASS（samples=339）
  3. 关键切主检查均命中：`backend=factor_graph`、`active_backend_source=factor_graph`、`fg_shadow_mode=false`
  4. 验收记录：`perf_reports/results/localization_fg_mainline_20260320_094935/acceptance_report.md`

#### Localization M4 / M5
- **2026-03-15 复核后状态**:
  1. `inject_scenario.py` / `localization_health_check.sh` / `localization_log_check.sh` 已落地
  2. `compare_backends.py` 已支持时长、输出路径、同步阈值参数
  3. `cone_gt_annotator.py` 与 `localization_regression_ci.yaml` 已补齐
  4. 仍缺标准场景数据集、KPI 阈值与系统级自动化验收流程

### 3.3 规划模块

#### Planning M1
- **当前仍未实现**:
  1. 实车联调验收（端到端回放已具备脚本与记录，实车切主尚未收口）
  2. `skidpad` 回放稳定性收敛（当前存在 replay 级随机抖动，需多轮稳定通过）

- **2026-03-18 本轮补充**:
  1. `planning_pipeline`（line/skidpad/high_speed）已接入 `PathLimitsV1/V2` 双发布主链
  2. `fsd_launch/subsystems/{mission_stack,planning}.launch` 与 `planning_pipeline.launch` 已支持 `output_pathlimits_v2_topic` 和兼容开关透传
  3. 新增 `planning_ros/test/test_pathlimits_v2_contract.cpp` 覆盖 V1->V2 转换边界与数组契约
  4. `control_ros` 已支持 `PathLimitsV1/V2` 并行订阅、优先级切换与 `V2->V1` 自动回退框架（默认保持 V1 优先，避免行为突变）
  5. `trackdrive/autocross/acceleration/skidpad` 任务入口已暴露控制侧切主开关，支持按 mission 粒度执行 V2 切主/回退验收
  6. 已新增 `scripts/validate_pathlimits_v2_cutover_replay.sh`，支持 `v2_primary` 与 `v2_fallback_no_v2_publish` 双场景回放验收并输出 `results.tsv/summary.md/results.json`
  7. 已修复回放验收脚本在大日志下的解析问题（`Argument list too long`）并补齐 `PathLimitsV2 recovered` 识别
  8. 已新增 `scripts/run_pathlimits_v2_cutover_matrix.sh`，支持多 mission 矩阵化执行并输出 `matrix.tsv/summary.md/matrix.json`
  9. 已完成四 mission 回放验收记录，见 `perf_reports/results/pathlimits_v2_cutover_20260318/acceptance_report.md`；其中 `trackdrive/autocross/acceleration` 稳定 PASS，`skidpad` 需继续收敛稳定性

#### Planning M2-M4
- **当前仍未实现**:
  1. Autocross Boundary Graph / Corridor / Centerline Optimizer
  2. 直线加速赛分段速度规划 V2 输出
  3. 八字绕环双圆增强、圈次状态机、相位速度控制的设计收口

#### Planning M5
- **修订后判断**: 已验证冻结。
- **剩余收尾**:
  1. 遗留代码清理
  2. 实车验证
  3. 回顾与文档归档

### 3.4 系统级

#### Audit Fixes Progress
- **修订前误差**: 旧文档仍写“进行中”。
- **修订后判断**: 22/22 已完成。
- **注意**: 原文中的“待测试/未提交”等措辞属于执行期快照，不应再作为当前项目状态引用。

---

## 四、当前最值得继续推进的真实工作

### P0
1. perception baseline 已完成首版冻结并打通脚本闭环；`track/skidpad` GT 已完成清洗 v2，下一步是 `accel` GT 清洗与门限继续收紧
2. 完成 EBS/VCU 协议映射、台架验证与故障安全策略
3. 颜色语义代码收口、fused 链路补齐、阈值冻结与 CI 门禁已完成；下一步按业务目标继续优化 `track` 一致率
4. `acceleration control_mode=2` 已完成代码侧一致性固化（含测试）；下一步与车端协议联调确认

### P1
1. 补齐 localization 标准场景数据并接入系统级回归验收
2. 将 FG 主后端验收从 `trackdrive` 扩展到 `autocross/acceleration/skidpad` 并接入门禁
3. 推进 `HUAT_PathLimitsV2` 实车切主验收，并收敛 `skidpad` 回放稳定性

### P2
1. 继续扩展实车验证
2. 将历史计划文档和现状文档进一步分层，减少再次漂移

---

## 五、建议使用方式

- 需要看“现在哪些事情真没做完”：优先读 `docs/remaining_work_audit_2026-03-06.md`
- 需要看“某个设计最初是怎么设想的”：再回看对应 `docs/modules/*` 与 `docs/plans/*`
- 需要看“历史改造过程”：回看 `HANDOFF.md`、`RESTRUCTURE_PLAN.md`、`docs/audit_fixes_progress.md`
